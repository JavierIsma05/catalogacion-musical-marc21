{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Formulario MARC21 Musical</title>
    <link rel="stylesheet" href="{% static 'catalogacion/css/styles.css' %}">
</head>

<body>

<header>
    <h2>Modificar Obra</h2>
    <p>Datos de Cabecera o Líder</p>
</header>

<div class="container">

    <!-- Mensajes Django -->
    {% if messages %}
        <div class="messages">
            {% for message in messages %}
                <div class="alert {{ message.tags }}">{{ message }}</div>
            {% endfor %}
        </div>
    {% endif %}

    <form method="POST" enctype="multipart/form-data">
        {% csrf_token %}

        <!-- 🔸 CABECERA -->
        <fieldset class="fieldset-block">
            <legend>Datos de Cabecera</legend>
            <div class="field-group">
                <label>Estado del Registro</label>
                {{ form.estado_registro }}
            </div>
            <div class="field-group">
                <label>Tipo de Registro</label>
                {{ form.tipo_registro }}
            </div>
            <div class="field-group">
                <label>Nivel Bibliográfico</label>
                {{ form.nivel_bibliografico }}
            </div>
            <div class="field-group">
                <label>Nivel de Codificación</label>
                {{ form.nivel_codificacion }}
            </div>
            <div class="field-group">
                <label>Forma de Catalogación Descriptiva</label>
                {{ form.forma_catalogacion }}
            </div>
        </fieldset>

        <!-- 🔹 RESTO DE CAMPOS -->
        <fieldset class="fieldset-block">
            <legend>008 - Códigos de información</legend>
            <div class="field-group"><label>Tipo de fecha / estado</label>{{ form.tipo_fecha_publicacion }}</div>
            <div class="field-group"><label>Primera fecha</label>{{ form.primera_fecha }}</div>
            <div class="field-group"><label>Segunda fecha</label>{{ form.segunda_fecha }}</div>
            <div class="field-group"><label>Lugar de publicación</label>{{ form.lugar_publicacion }}</div>
            <div class="field-group"><label>Forma de la composición</label>{{ form.forma_composicion }}</div>
            <div class="field-group"><label>Lengua</label>{{ form.lengua }}</div>
        </fieldset>

        <fieldset class="fieldset-block">
            <legend>031 - Información del incipit musical</legend>
            <div class="field-group"><label>Título o encabezamiento</label>{{ form.titulo_encabezamiento }}</div>
            <div class="field-group"><label>Instrumento</label>{{ form.instrumento }}</div>
            <div class="field-group"><label>Notación musical (token)</label>{{ form.notacion_token }}</div>
        </fieldset>

        <fieldset class="fieldset-block">
            <legend>092 - Clasificación local (COTA)</legend>
            <div class="field-group"><label>$a</label>{{ form.clasificacion_a }}</div>
            <div class="field-group"><label>$b</label>{{ form.clasificacion_b }}</div>
            <div class="field-group"><label>$c</label>{{ form.clasificacion_c }}</div>
        </fieldset>

        <fieldset class="fieldset-block">
            <legend>Archivos</legend>
            <div class="field-group"><label>Portada</label>{{ form.portada }}</div>
            <div class="field-group"><label>Documento</label>{{ form.documento }}</div>
        </fieldset>

        <div class="field-group">
            <label>Publicado</label>
            {{ form.publicado }}
        </div>

        <div class="save-buttons">
            <button type="submit">Guardar</button>
            <button type="reset">Cancelar</button>
        </div>
    </form>
</div>

</body>
</html>
